import{Link}from'react-router-dom';import{Product,catName}from'../data/products';import{useApp}from'../context/AppContext';import{status,t}from'../utils/i18n';import{ImageBox}from'./SmartImage';
export function ProductCard({p}:{p:Product}){const{lang,add}=useApp();return <article className="card"><ImageBox src={p.images.cover} label={p.productNo}/><div className="pad"><p className="eyebrow">{p.productNo} · {catName(p.category,lang)}</p><h3>{p.titleEn}</h3><p>{p.titleZh}</p><p><b>{status(p.availability,lang)}</b></p><p>{p.colors.join(' / ')}</p><p>{p.sizes.join(' · ')}</p><div className="actions"><Link className="btn ghost" to={`/products/${p.id}`}>{t[lang].view}</Link><button className="btn" onClick={()=>add({productId:p.id,color:p.colors[0],size:p.sizes[0],quantity:100,note:''})}>{t[lang].add}</button></div></div></article>}
