import{useState}from'react';export function ImageBox({src,label,ai=false}:{src?:string;label:string;ai?:boolean}){const[bad,setBad]=useState(!src);return bad?<div className="placeholder"><span>XUANYANG</span><b>{label}</b><small>{ai?'AI Model Preview Coming Soon':'Image placeholder - replace in products.ts'}</small></div>:<img loading="lazy" src={src} alt={`${label} XUANYANG`} onError={()=>setBad(true)}/>}
