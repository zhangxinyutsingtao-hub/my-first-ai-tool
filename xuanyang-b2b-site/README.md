# XUANYANG B2B Site

Vite + React + TypeScript independent brand catalog and wholesale inquiry site.

```bash
npm install
npm run dev
npm run build
```

See `docs/` for Chinese maintenance and deployment instructions.
